/** Ambient type for the fork's virtual engine module — the esbuild-bundled IIFE string
 * produced by apps/web/fork/vitePluginForkDesignMode.ts. */
declare module "virtual:fork-design-mode-engine" {
  const code: string;
  export default code;
}
