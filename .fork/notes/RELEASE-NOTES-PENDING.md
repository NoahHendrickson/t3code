# Pending release-note lines

Lines to fold into the next fork release's notes, then delete from here.
Each entry names the sync or PR that created the obligation.

- fix(web): background preview guests no longer show through the Cool Darker glass while an agent drives them (fork-glass-preview-parking, fixes a v0.1.18 regression from upstream #9001)
